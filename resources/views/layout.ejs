<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Pizza</title>
    <base href="/" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Lato:ital,wght@0,700;1,300;1,400&family=Ubuntu&family=Varela+Round&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="https://maxst.icons8.com/vue-static/landings/line-awesome/line-awesome/1.3.0/css/line-awesome.min.css">
    
    <link rel="stylesheet" href="/css/app.css" type="text/css" />
  </head>
  <body>
    <nav class="container mx-auto flex flex-col md:flex-row items-center justify-center md:justify-between py-4 px-2">
      <div>
        <a href="/">
          <img src="/img/logo.png" alt="logo" />
        </a>
      </div>
      <div>
        <ul class="flex items-center">
          <li class="mx-4 md:mx-6"><a href="#">Menu</a></li>
          <% if(user){ %> 
            
            <% if(user.role==="customer"){ %>

              <li class="mx-4 md:mx-6"><a href="/customer/orders">Orders</a></li>

            <% }else{ %>

              <li class="mx-4 md:mx-6"><a href="/admin/orders">Get Orders</a></li>
            <% } %>
            <li class="mx-4 md:mx-6">
              <form id="logout" action="/logout" method="POST">
                <a onclick="document.getElementById('logout').submit()" href="#"
                  >Logout</a
                >
            </form>
          </li>

          <%} else{ %>
          <li class="mx-4 md:mx-6"><a href="/getotp">Register</a></li>
          <li class="mx-4 md:mx-6"><a href="/login">Login</a></li>

          <% } %>
          <li class="mx-4 md:mx-6">
            <a
              href="/cart"
              class="inline-block px-4 py-2 rounded-full flex items-center"
            >
              <span id="cartCounter" class="text-white font-bold pr-2"
                ><%= session.cart ? session.cart.totalQty : "" %></span
              >
              <img src="/img/cart.png" alt="cart" />
            </a>
          </li>
        </ul>
      </div>
    </nav>
    <%- body %>
    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/app.js"></script>
  </body>
</html>
